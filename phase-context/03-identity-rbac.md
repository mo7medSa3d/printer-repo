# Phase 03 Context
Implemented in this pass: manager_sessions carries user_id and role; login can authenticate users against users + tenant_users; session validation rechecks membership/role.
Legacy environment manager credentials remain an owner bootstrap path. Full invitation/SSO lifecycle remains outside this archive.
