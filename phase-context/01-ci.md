# Phase 01 Context
Objective: establish clean build/test baseline.
Blocked evidence: npm dependency installation cannot complete because the execution environment is Node 22 while package requires >=24.15.0, and registry fetches return EAI_AGAIN.
Do not weaken engine requirements or CI gates.
