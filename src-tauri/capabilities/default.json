{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Odoo Print Manager main window (least privilege).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default"
  ]
}
