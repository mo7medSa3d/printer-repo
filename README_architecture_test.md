The architecture limits have been verified. Odoo controls logical entities like branch and destination. Gateway does not maintain parallel business structure.
Gateway holds agent, printer, and queue.
There are tests checking rejection of unsupported fields such as "branch_id" at registration.
